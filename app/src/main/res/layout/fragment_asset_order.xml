<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F7F8FA">

    <!-- Loading Spinner -->
    <ProgressBar
        android:id="@+id/progressBarAsset"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_gravity="center"
        android:indeterminateTint="#4A90E2"
        android:visibility="gone" />

    <!-- Content Layout -->
    <LinearLayout
        android:id="@+id/contentLayoutAsset"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp"
        android:visibility="gone">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageView
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_marginEnd="8dp"
                android:contentDescription="Asset Order Icon"
                android:src="@drawable/inventory_icon"
                app:tint="#4A90E2" />

            <TextView
                android:id="@+id/textViewTitleAsset"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Asset Order"
                android:textColor="#1E1E1E"
                android:textSize="20sp"
                android:textStyle="bold"
                android:letterSpacing="0.02" />
        </LinearLayout>

        <!-- Search Bar -->
        <androidx.appcompat.widget.AppCompatEditText
            android:id="@+id/editSearchAsset"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:layout_marginBottom="8dp"
            android:hint="Search by name"
            android:imeOptions="actionSearch"
            android:inputType="text"
            android:padding="10dp"
            android:drawableStart="@drawable/ic_search"
            android:drawablePadding="8dp"
            android:drawableEnd="@drawable/ic_clear"
            android:background="@drawable/bg_search_rounded"/>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerViewAsset"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:padding="12dp"
            android:scrollbars="vertical" />

    </LinearLayout>

    <!-- Floating Buttons -->
    <LinearLayout
        android:id="@+id/fabContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:orientation="vertical"
        android:padding="16dp">

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/btnScan"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:contentDescription="Scan"
            android:src="@drawable/ic_scan"
            app:backgroundTint="@color/blue_primary" />
    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
